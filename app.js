const express=require('express');
const body_parser=require('body-parser');
app.use(body_parser.json());
const app=express();
module.exports=app;
const express=require('express');
const app=express();
module.exports=app;